name: End-to-end tests 🧪
on: push
jobs:
  cypress-run:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Cypress run
        uses: cypress-io/github-action@v6
        env:
          CYPRESS_RECORD_KEY: ${{ secrets.88028c5b-22f7-4139-9d91-9bb8dfbf7d66}}
        with:
          command: npm run teste:cloud